(()=>{const suits=['♠','♥','♦','♣'],ranks=['A','K','Q','J','10','9','8','7','6','5','4','3','2'];let deck=[],player=[],dealer=[],creditsVal=1000,betVal=50,currentBet=50,live=false;function mk(){deck=[];for(const s of suits)for(const r of ranks)deck.push({r,s});deck.sort(()=>Math.random()-.5)}function val(hand){let total=0,aces=0;hand.forEach(c=>{if(c.r==='A'){aces++;total+=11}else if(['K','Q','J'].includes(c.r))total+=10;else total+=+c.r});while(total>21&&aces){total-=10;aces--}return total}function card(c,hide){return `<div class="card ${c.s==='♥'||c.s==='♦'?'red':''}">${hide?'?':c.r+'<br>'+c.s}</div>`}function render(show=false){dealerHand.innerHTML=dealer.map((c,i)=>!show&&live&&i===1?card(c,true):card(c)).join('');playerHand.innerHTML=player.map(c=>card(c)).join('');dealerTotal.textContent=(!show&&live)?'Total: ?':'Total: '+val(dealer);playerTotal.textContent='Total: '+val(player);credits.textContent=creditsVal;bet.textContent=betVal;mainScore.textContent=creditsVal;stateText.textContent=live?'LIVE':'READY';}function end(msg,pay){creditsVal+=pay;result.textContent=msg+(pay?' +'+pay:'');live=false;render(true)}function deal(){if(live)return;if(creditsVal<betVal){result.textContent='Not enough credits';return}mk();currentBet=betVal;creditsVal-=currentBet;player=[deck.pop(),deck.pop()];dealer=[deck.pop(),deck.pop()];live=true;result.textContent='Play hand';if(val(player)===21)end('BLACKJACK',currentBet*2.5);else render()}function hit(){if(!live)return;player.push(deck.pop());if(val(player)>21)end('BUST',0);else render()}function stand(){if(!live)return;while(val(dealer)<17)dealer.push(deck.pop());let p=val(player),d=val(dealer);if(d>21||p>d)end('WIN',currentBet*2);else if(p===d)end('PUSH',currentBet);else end('DEALER WINS',0)}function dbl(){if(!live||player.length!==2)return;if(creditsVal<currentBet){result.textContent='Need more credits to double';return}creditsVal-=currentBet;currentBet*=2;player.push(deck.pop());if(val(player)>21)end('BUST',0);else stand()}dealBtn.onclick=deal;hitBtn.onclick=hit;standBtn.onclick=stand;doubleBtn.onclick=dbl;betUp.onclick=()=>{if(!live&&betVal<500){betVal+=25;render(true)}};betDown.onclick=()=>{if(!live&&betVal>25){betVal-=25;render(true)}};render(true)})();