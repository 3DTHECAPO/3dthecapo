const STARTING_CREDITS=1000,BETS=[25,50,100,250],STORAGE_KEY='play3d_production_v1';const SYMBOLS=[{id:'speaker',src:'assets/speaker.png',kind:'object',weight:14,payout:3},{id:'crown',src:'assets/crown.png',kind:'object',weight:8,payout:8},{id:'key',src:'assets/key.png',kind:'object',weight:9,payout:7},{id:'lock',src:'assets/lock.png',kind:'object',weight:9,payout:7},{id:'vault',src:'assets/vault.png',kind:'object',weight:7,payout:10},{id:'chain',src:'assets/chain.png',kind:'object',weight:12,payout:4},{id:'mic',src:'assets/mic.png',kind:'object',weight:10,payout:5},{id:'hoodie',src:'assets/hoodie.png',kind:'object',weight:9,payout:5},{id:'cash',src:'assets/cash.png',kind:'object',weight:13,payout:4},{id:'grammy',src:'assets/cover-fuck-a-grammy.png',kind:'cover',weight:4,payout:16},{id:'100x3',src:'assets/cover-100x3.png',kind:'cover',weight:4,payout:18},{id:'resume',src:'assets/cover-my-resume.png',kind:'cover',weight:4,payout:18},{id:'logo',src:'assets/logo.png',kind:'logo',weight:2,payout:50}];let state=loadState(),spinning=false;const els={reels,credits:creditsValue,bet:betValue,lastWin:lastWinValue,status:statusValue,jackpot:jackpotValue,message:messageBar};function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);if(raw){const p=JSON.parse(raw);return{credits:Number.isFinite(p.credits)?p.credits:STARTING_CREDITS,betIndex:Number.isInteger(p.betIndex)?Math.max(0,Math.min(BETS.length-1,p.betIndex)):0,jackpot:Number.isFinite(p.jackpot)?p.jackpot:125000,lastWin:Number.isFinite(p.lastWin)?p.lastWin:0}}}catch(e){}return{credits:STARTING_CREDITS,betIndex:0,jackpot:125000,lastWin:0}}function saveState(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}function pickSymbol(){const total=SYMBOLS.reduce((s,x)=>s+x.weight,0);let t=Math.random()*total;for(const x of SYMBOLS){t-=x.weight;if(t<=0)return x}return SYMBOLS[0]}function buildBoard(){return Array.from({length:3},()=>Array.from({length:5},()=>pickSymbol()))}function makeCell(symbol){const cell=document.createElement('div');cell.className='cell '+symbol.kind+(symbol.kind==='object'?' object':'');const frame=document.createElement('div');frame.className='cellFrame';const img=document.createElement('img');img.src=symbol.src;img.alt='';frame.appendChild(img);cell.appendChild(frame);return cell}function renderBoard(board){els.reels.innerHTML='';for(let c=0;c<5;c++){const reel=document.createElement('div');reel.className='reel';for(let r=0;r<3;r++)reel.appendChild(makeCell(board[r][c]));els.reels.appendChild(reel)}}function updateUi(status='READY',message=''){els.credits.textContent=state.credits.toLocaleString();els.bet.textContent=BETS[state.betIndex].toLocaleString();els.lastWin.textContent=state.lastWin.toLocaleString();els.status.textContent=status;els.jackpot.textContent=Math.round(state.jackpot).toLocaleString();els.message.textContent=message}function evaluate(board){const bet=BETS[state.betIndex];let total=0;for(const line of [board[0],board[1],board[2]]){const counts={};line.forEach(s=>counts[s.id]=(counts[s.id]||0)+1);for(const s of SYMBOLS){const count=counts[s.id]||0;if(count>=3){const mult=count===5?s.payout*2:count===4?s.payout*1.4:s.payout;total+=Math.round(bet*mult)}}}return total}function maybeRewardFromSlot(win){if(!window.Play3DRewards||win<=0)return;const bet=BETS[state.betIndex];if(win>=bet*8){const reward=Play3DRewards.maybeReward({game:'slots',trigger:'big_win',chance:.14,tier:win>=bet*15?'premium':'standard'});if(reward&&reward.type==='chips'&&reward.chips){state.credits+=reward.chips;saveState()}}if(win>=bet){Play3DRewards.recordCounter('slots_win_streak',1)}else{Play3DRewards.resetCounter('slots_win_streak')}if(Play3DRewards.getCounter('slots_win_streak')>=3){const reward=Play3DRewards.maybeReward({game:'slots',trigger:'win_streak',chance:.18,tier:'standard'});if(reward&&reward.type==='chips'&&reward.chips){state.credits+=reward.chips;saveState()}Play3DRewards.resetCounter('slots_win_streak')}}function spinOnce(){if(spinning)return;const bet=BETS[state.betIndex];if(state.credits<bet){updateUi('NO CREDITS','OUT OF CREDITS');return}spinning=true;state.credits-=bet;state.jackpot+=Math.round(bet*.35);state.lastWin=0;updateUi('SPINNING','');const liveReels=Array.from(document.querySelectorAll('.reel'));const timers=liveReels.map((reel,index)=>setInterval(()=>{reel.innerHTML='';for(let row=0;row<3;row++)reel.appendChild(makeCell(pickSymbol()))},88+index*18));setTimeout(()=>{const finalBoard=buildBoard();timers.forEach((timer,index)=>setTimeout(()=>clearInterval(timer),index*140));setTimeout(()=>{renderBoard(finalBoard);const win=evaluate(finalBoard);state.lastWin=win;if(win>0){state.credits+=win;maybeRewardFromSlot(win);updateUi('WIN','WON '+win.toLocaleString())}else{window.Play3DRewards&&Play3DRewards.resetCounter('slots_win_streak');updateUi('READY','')}saveState();spinning=false},620)},520)}downBetBtn.addEventListener('click',()=>{if(spinning)return;state.betIndex=Math.max(0,state.betIndex-1);saveState();updateUi()});upBetBtn.addEventListener('click',()=>{if(spinning)return;state.betIndex=Math.min(BETS.length-1,state.betIndex+1);saveState();updateUi()});spinBtn.addEventListener('click',spinOnce);autoBtn.addEventListener('click',()=>{let spinsLeft=10;const run=()=>{if(spinsLeft<=0||spinning||state.credits<BETS[state.betIndex])return;spinOnce();spinsLeft-=1;const wait=setInterval(()=>{if(!spinning){clearInterval(wait);run()}},120)};run()});renderBoard(buildBoard());updateUi();